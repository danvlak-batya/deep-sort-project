# Colab-friendly dependencies (torch is preinstalled in Colab)
numpy>=1.22,<2.1
opencv-python
scipy
filterpy
PyYAML
scikit-learn
Pillow
tqdm
ultralytics
transformers
trackeval

# torchreid: install separately in Colab (PyPI package fails on Python 3.12):
#   pip install git+https://github.com/KaiyangZhou/deep-person-reid.git
